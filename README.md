# Wikelo_resource_database

The master database contains all information on the items including who has them and how many each person has. The simplified database just contains the item name and quantity. The master database is the only one that gets directly updated by the system, always check if the commit version is the same on the simplified database otherwise it is out of date.
